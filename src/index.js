const controller = require('./controller'),
  helpers = require('./helpers'),
  models = require('./models'),
  store = require('./store');

module.exports = {
  controller,
  helpers,
  models,
  store
};
